services:
  polar-to-fittrackee:
    build: .
    container_name: polar-to-fittrackee
    command: ["uv", "run", "sync.py"]
    volumes:
      - ./data:/usr/src/app/data
      - ./config:/usr/src/app/config
    restart: no
